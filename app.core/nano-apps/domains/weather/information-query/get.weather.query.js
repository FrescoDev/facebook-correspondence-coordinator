/**
 * Gets the weather based on some paramters provided by the request source.
 * @param  {object} queryParams The paramerters required to fulfill the query.
 */
const getWeatherQuery = (queryParams) => {
    return 'weather is 20 Degrees'
}

export default getWeatherQuery;